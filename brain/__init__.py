"""Brain v0.2 — the neural network core of Brægn Brain.

This package implements ONLY the model: tokenizer, transformer, inference.
It knows nothing about FastAPI, databases, memory, tools, or the frontend —
those are separate systems (deferred; see Brain_v0.2_Audit_and_Gate_Report.md).
"""

__version__ = "0.2.0"
